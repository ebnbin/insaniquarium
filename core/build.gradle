import Dependency

apply plugin: 'kotlin'

java.toolchain.languageVersion.set(JavaLanguageVersion.of(Version.JAVA))

dependencies {
  api Dependency.GDX_FREETYPE
  api Dependency.GDX
  api project(':kgdx')
}
