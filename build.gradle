import Dependency

buildscript {
  repositories {
    mavenCentral()
    google()
  }
  dependencies {
    classpath Dependency.GRADLE
    classpath Dependency.KOTLIN_GRADLE_PLUGIN

  }
}

subprojects {
  repositories {
    mavenCentral()
  }
}
